spring:
  application:
    name: forohub
  datasource:
      url: jdbc:mysql://127.0.0.1:3309/forohub?createDatabaseIfNotExist=true
      username: root
      password: AluraLatamForoHub
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    # show-sql: true
    # properties:
      # hibernate:
        # format_sql: true
