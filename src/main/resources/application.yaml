spring:
  application:
    name: forohub
  datasource:
      url: ${FOROHUB_DB_URL:jdbc:mysql://127.0.0.1:3309/forohub?createDatabaseIfNotExist=true}
      username: ${FOROHUB_DB_USER:root}
      password: ${FOROHUB_DB_PASSWORD:AluraLatamForoHub}
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    # show-sql: true
    # properties:
      # hibernate:
        # format_sql: true

api:
  security:
    secret: ${FOROHUB_JWT_SECRET:forohub-secret-dev}
    hash-secret: ${FOROHUB_HASH_SECRET:forohub-hash-secret-dev}
    bcrypt-strength: ${FOROHUB_BCRYPT_STRENGTH:10}
